#include "main.h"
/**
* _abs - sxript that returns absolute value of a number
* @x: variable used for script
* Return: x
*
*/

int _abs (int x)
{
	if (x < 0)
	{
	x = (-1) * x;
	return (x);
	}
	else
		return(x);


}
